import styled from '@emotion/styled';

const Menu = styled.ul`
  position: relative;
  margin: 0;
  padding: 4px 0;
  list-style: none;
  overflow-x: hidden;
  overflow-y: auto;
`;

export default Menu;
