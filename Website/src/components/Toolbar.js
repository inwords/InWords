import styled from '@emotion/styled';

const Toolbar = styled.div`
  position: relative;
  display: flex;
  align-items: center;
  padding-left: 24px;
  padding-right: 24px;
  min-height: 64px;
`;

export default Toolbar;
