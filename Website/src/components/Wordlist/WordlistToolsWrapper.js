import React from 'react';

function WordlistToolsWrapper({ children }) {
    return (
        <div className="bg-light p-2 mb-3">
            {children}
        </div>
    );
}

export default WordlistToolsWrapper;
