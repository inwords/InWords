import styled from '@emotion/styled';

const ListItemButtonBase = styled.div`
  display: flex;
  align-items: center;
  justify-content: flex-start;
  cursor: pointer;
  user-select: none;
  text-decoration: none;
`;

export default ListItemButtonBase;
