import React from 'react';
import PageLayout from './PageLayout';

function PageLayoutContainer({ ...rest }) {
    return (
        <PageLayout {...rest} />
    );
}

export default PageLayoutContainer;
