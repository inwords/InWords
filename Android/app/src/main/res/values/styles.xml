<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">?attr/colorPrimary</item>

        <!-- ActionMode -->
        <item name="actionModeStyle">@style/ActionModeStyle</item>
        <item name="actionModeCloseButtonStyle">@style/ActionModeCloseButtonStyle</item>
        <item name="actionMenuTextColor">@color/text_color_default_inverse</item>
        <item name="windowActionModeOverlay">true</item>

        <!-- SearchView -->
        <item name="searchViewStyle">@style/AppSearchViewStyle</item>

        <!-- BottomSheet -->
        <item name="bottomSheetDialogTheme">@style/AppBottomSheetDialogTheme</item>
    </style>

    <style name="ActionModeStyle" parent="Widget.AppCompat.ActionMode">
        <item name="background">@color/colorPrimary</item>
        <item name="titleTextStyle">@style/TextAppearance.Inwords.Title.Regular.Inverse</item>
    </style>

    <style name="ActionModeCloseButtonStyle" parent="Widget.AppCompat.Light.ActionButton.CloseMode">
        <item name="android:tint">@color/text_color_default_inverse</item>
    </style>

    <style name="ToolbarStyle">
        <item name="android:background">@color/white</item>
        <item name="android:theme">@style/ToolbarTheme</item>
        <item name="android:titleTextStyle">@style/TextAppearance.Inwords.Title.Regular.Inverse
        </item>
    </style>

    <style name="ToolbarTheme" parent="AppTheme">
        <item name="colorControlNormal">@color/text_color_default</item>
    </style>

    <style name="AppSearchViewStyle" parent="Widget.AppCompat.SearchView">
        <item name="queryBackground">@android:color/transparent</item>
        <item name="searchIcon">@drawable/ic_search_icon_secondary_24dp</item>
        <item name="searchHintIcon">@drawable/ic_search_icon_secondary_24dp</item>
        <item name="closeIcon">@drawable/ic_clear_icon_secondary_24dp</item>
        <item name="queryHint">@string/dictionary_search_hint</item>
        <item name="android:imeActionId">6</item>
    </style>

    <style name="AppBottomSheetDialogTheme" parent="Theme.MaterialComponents.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
    </style>

    <style name="AppModalStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/rounded_dialog</item>
    </style>

    <style name="AuthorisationButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">32dp</item>
        <item name="pb_textComplete">"Успешно"</item>
        <item name="pb_colorComplete">@color/green_complete</item>
        <item name="pb_colorNormal">@color/colorPrimary</item>
        <item name="pb_colorPressed">@color/blue_pressed</item>
        <item name="pb_colorProgress">@color/purple_progress</item>
    </style>

    <style name="TextAppearance.Inwords.Link" parent="TextAppearance.MaterialComponents.Body2">
        <item name="android:textColor">@color/colorSecondary</item>
    </style>

    <declare-styleable name="ProcessButton">
        <attr name="pb_textProgress" format="string" />
        <attr name="pb_textComplete" format="string" />
        <attr name="pb_textError" format="string" />
        <attr name="pb_colorProgress" format="color" />
        <attr name="pb_colorComplete" format="color" />
        <attr name="pb_colorError" format="color" />
    </declare-styleable>

    <declare-styleable name="FlatButton">
        <attr name="pb_colorPressed" format="color" />
        <attr name="pb_colorNormal" format="color" />
        <attr name="pb_cornerRadius" format="dimension" />
    </declare-styleable>
</resources>
